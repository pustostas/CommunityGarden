@model IEnumerable<CommunityGarden.Models.PlotUser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlotId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GardenUserId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlotId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GardenUserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlotUserId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlotUserId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlotUserId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
